# Configurações locais do Claude Code (não fazem parte do projeto)
.claude/

# Documentos de referência internos — nunca versionar (podem conter dados
# sensíveis, ex.: planilhas de infraestrutura crítica, contratos reais)
Docs/

# Dependências
app/node_modules/

# Build
app/dist/
app/*.tsbuildinfo
app/vite.config.js
app/vite.config.d.ts

# Saida dos testes de ponta a ponta (Playwright)
app/test-results/
app/playwright-report/
app/blob-report/

# Sistema
.DS_Store
Thumbs.db

# Firebase
*-debug.log
.firebase/

# Credenciais locais — nunca versionar (§5.3 do Prompt Mestre v2.1)
serviceAccount*.json
*.serviceaccount.json
.env
.env.local
